import React, { Component } from 'react'
import Main from './Main'

export default class App extends Component {
  state = {
  
  }
  
  render() {
    return (
      <Main app={this} />
    )
  }
}

